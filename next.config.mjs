/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  experimental: {
    staticPageGenerationTimeout: 180,
  },
};

export default nextConfig;